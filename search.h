#ifndef SEARCH_H
#define SEARCH_H
#include <vector>
using namespace std;
// Recursive linear search (find last occurrence)

int recursiveLastOccurrence(const vector<int>& vec, int target, int index);
#endif
#pragma once
